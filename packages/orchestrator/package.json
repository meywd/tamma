{
  "name": "@tamma/orchestrator",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist .tsbuildinfo",
    "typecheck": "tsc --noEmit",
    "migrate:latest": "node --loader ts-node/esm database/migrate.ts latest",
    "migrate:rollback": "node --loader ts-node/esm database/migrate.ts rollback"
  },
  "dependencies": {
    "@tamma/shared": "workspace:*",
    "@tamma/events": "workspace:*",
    "@tamma/observability": "workspace:*",
    "@tamma/providers": "workspace:*",
    "@tamma/platforms": "workspace:*",
    "@tamma/intelligence": "workspace:*",
    "@tamma/gates": "workspace:*",
    "@tamma/workers": "workspace:*",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "typescript": "~5.7.2"
  }
}
