# Story 1.5-11: GitHub App Authentication & Installation Management

**Epic:** 1.5 - Deployment, Packaging & Operations
**Status:** Ready for Development
**Priority:** MVP Critical (SaaS Enabler)
**Estimated Effort:** 5 days

## ⚠️ MANDATORY: Before You Code

**ALL contributors MUST read and follow the comprehensive development process:**

📖 **[BEFORE_YOU_CODE.md](../../BEFORE_YOU_CODE.md)**

This mandatory guide includes:
- 7-Phase Development Workflow (Read → Research → Break Down → TDD → Quality Gates → Failure Handling)
- Knowledge Base Usage (.dev/ directory: spikes, bugs, findings, decisions)
- TRACE/DEBUG Logging Requirements for all functions
- Test-Driven Development (TDD) mandatory workflow
- 100% Test Coverage requirement
- Build Success enforcement
- Automatic retry and developer alert procedures

**Failure to follow this process will result in rework.**

## Story

As a **platform operator deploying Tamma as a SaaS service**,
I want GitHub App authentication with installation lifecycle management,
so that multiple organizations can install Tamma from the GitHub Marketplace and receive automated development workflows without sharing personal access tokens.

## Overview

Implement dual authentication support for the GitHub platform: the existing PAT-based auth for self-hosted deployments, and new GitHub App auth for SaaS deployments. GitHub App auth uses a two-tier model — a short-lived JWT (signed with the App's RSA private key) authenticates as the App itself, then per-installation tokens (scoped to specific org/repo installations) are obtained via the GitHub API. Installation tokens auto-expire after 1 hour and must be refreshed transparently.

This story also covers the installation callback flow (the redirect endpoint GitHub calls after a user installs the App), the database schema for persisting installations, and secure private key management via environment variables or file paths.

## Acceptance Criteria

1. GitHub platform supports dual auth modes: PAT (existing) and GitHub App (new), selected via configuration
2. GitHub App auth generates JWT signed with RSA private key using `@octokit/auth-app`
3. Installation tokens are auto-refreshed before expiration (with configurable buffer, default 5 minutes)
4. Installation callback endpoint (`GET /api/github/callback`) handles post-install redirect, persists installation to database
5. Installation database schema stores: `installation_id`, `account_login`, `account_type` (org/user), `app_id`, `permissions`, `repository_selection`, `created_at`, `suspended_at`
6. Private key can be provided via environment variable (`GITHUB_APP_PRIVATE_KEY`) or file path (`GITHUB_APP_PRIVATE_KEY_PATH`)
7. GitHub App configuration validated at startup — missing `appId`, `privateKey`, or `webhookSecret` produces clear error messages
8. Platform factory creates correct platform instance based on auth mode in config
9. Unit tests cover: JWT generation, token refresh, installation persistence, callback handling, config validation, factory selection
10. Integration tests cover: end-to-end installation callback flow, token lifecycle with mock GitHub API

## Tasks / Subtasks

- [ ] Task 1: Platform Dual Auth Support (AC: 1, 2, 3)
  - [ ] Subtask 1.1: Extend `GitHubPlatform` to accept `@octokit/auth-app` authentication strategy
  - [ ] Subtask 1.2: Implement `GitHubAppAuth` class wrapping `@octokit/auth-app` for JWT generation and installation token management
  - [ ] Subtask 1.3: Add auto-refresh logic with configurable expiry buffer (default 5 minutes before expiration)
  - [ ] Subtask 1.4: Ensure all Octokit API calls use the correct auth (App JWT for App-level, installation token for repo-level)

- [ ] Task 2: Shared Configuration Types (AC: 1, 6, 7)
  - [ ] Subtask 2.1: Add `GitHubAppConfig` type to `@tamma/shared` with fields: `appId`, `privateKey`, `privateKeyPath`, `webhookSecret`, `clientId`, `clientSecret`
  - [ ] Subtask 2.2: Add `authMode: 'pat' | 'github-app'` discriminator to `GitHubPlatformConfig`
  - [ ] Subtask 2.3: Add Zod validation schema for GitHub App config with clear error messages
  - [ ] Subtask 2.4: Support private key from env var (`GITHUB_APP_PRIVATE_KEY`) or file path (`GITHUB_APP_PRIVATE_KEY_PATH`)

- [ ] Task 3: CLI Configuration Support (AC: 6, 7)
  - [ ] Subtask 3.1: Add `tamma config set github.authMode github-app` CLI support
  - [ ] Subtask 3.2: Add `tamma config set github.appId <id>` and related App config commands
  - [ ] Subtask 3.3: Validate GitHub App config completeness on `tamma start` in SaaS mode

- [ ] Task 4: Installation Database Schema (AC: 5)
  - [ ] Subtask 4.1: Create migration for `github_installations` table with columns: `id` (PK), `installation_id` (unique), `app_id`, `account_login`, `account_type`, `target_type`, `repository_selection`, `permissions` (JSONB), `events` (JSONB), `suspended_at`, `created_at`, `updated_at`
  - [ ] Subtask 4.2: Add indexes on `installation_id`, `account_login`, `app_id`
  - [ ] Subtask 4.3: Create migration for `installation_repositories` join table (installation_id, repository_full_name)

- [ ] Task 5: Installation Persistence Layer (AC: 4, 5)
  - [ ] Subtask 5.1: Define `IInstallationStore` interface with methods: `save()`, `findById()`, `findByAccountLogin()`, `listActive()`, `markSuspended()`, `remove()`
  - [ ] Subtask 5.2: Implement `PgInstallationStore` using PostgreSQL
  - [ ] Subtask 5.3: Implement `InMemoryInstallationStore` for testing

- [ ] Task 6: Installation Callback Route (AC: 4)
  - [ ] Subtask 6.1: Implement `GET /api/github/callback` Fastify route handler
  - [ ] Subtask 6.2: On callback, fetch installation details from GitHub API, persist to database
  - [ ] Subtask 6.3: Redirect user to success page or dashboard after installation
  - [ ] Subtask 6.4: Handle error cases: invalid installation_id, GitHub API failure, duplicate installation

- [ ] Task 7: Platform Factory (AC: 8)
  - [ ] Subtask 7.1: Implement `GitHubPlatformFactory.create(config)` that returns PAT-based or App-based `GitHubPlatform` based on `authMode`
  - [ ] Subtask 7.2: Factory validates config completeness for selected auth mode
  - [ ] Subtask 7.3: Factory logs auth mode selection at startup

- [ ] Task 8: Tests (AC: 9, 10)
  - [ ] Subtask 8.1: Unit tests for `GitHubAppAuth` (JWT signing, token refresh, expiry handling)
  - [ ] Subtask 8.2: Unit tests for `PgInstallationStore` (CRUD operations, suspension, removal)
  - [ ] Subtask 8.3: Unit tests for callback route handler (success, error cases, duplicate handling)
  - [ ] Subtask 8.4: Unit tests for `GitHubPlatformFactory` (PAT mode, App mode, invalid config)
  - [ ] Subtask 8.5: Integration tests for end-to-end callback flow with mock GitHub API (MSW)
  - [ ] Subtask 8.6: Integration tests for token lifecycle (generation, refresh, expiry)

## Dev Notes

### ⚠️ Development Process Reminder

**Before implementing this story, ensure you have:**
1. ✅ Read [BEFORE_YOU_CODE.md](../../BEFORE_YOU_CODE.md)
2. ✅ Searched `.dev/` directory for related spikes, bugs, findings, and decisions
3. ✅ Reviewed relevant documentation in `docs/` directory
4. ✅ Checked existing code patterns for similar functionality
5. ✅ Planned TDD approach (Red-Green-Refactor cycle)

### Requirements Context Summary

**Epic Context:** This story is the foundational enabler for Tamma's SaaS deployment model. Without GitHub App auth, every user would need to create and manage their own PAT, which doesn't scale for a marketplace-style offering. GitHub App auth provides: org-level installation (not personal), fine-grained permissions, automatic token management, and webhook delivery tied to the App.

**Technical Context:** GitHub Apps use a two-tier authentication model:
1. **App-level JWT**: Signed with RSA private key, expires in 10 minutes. Used for App-level API calls (list installations, get installation details).
2. **Installation token**: Obtained by exchanging App JWT for an installation-scoped token. Expires in 1 hour. Used for all repo-level API calls (create PRs, read issues, push code).

The `@octokit/auth-app` library handles both tiers transparently, but we must ensure token refresh happens proactively (not on 401 failure).

**Integration Points:**
- Story 1.5-4 (Web Server): Provides the Fastify server for the callback endpoint
- Story 1.5-6 (Webhooks): Webhook handler must route `installation` events to the installation store
- Story 1.5-12 (SaaS Coordinator): Depends on this story for installation discovery
- Story 1-5 (GitHub Platform): Existing PAT-based implementation extended, not replaced

### Implementation Guidance

**Key Design Decisions:**

- **Dual auth, not replacement**: PAT auth remains for self-hosted users. Auth mode selected via config, not runtime detection.
- **`@octokit/auth-app` over manual JWT**: The official library handles JWT signing, installation token caching, and refresh. No reason to reimplement.
- **Private key as env var**: For container/SaaS deployments where file mounts are inconvenient. The env var holds the PEM-encoded key directly.
- **Installation DB over in-memory**: Installations must survive restarts. The SaaS coordinator (Story 1.5-12) queries this table to discover which repos to serve.

**Technical Specifications:**

- **Auth library**: `@octokit/auth-app` (official GitHub auth strategy)
- **JWT lifetime**: 10 minutes (GitHub maximum)
- **Installation token lifetime**: 1 hour (GitHub fixed)
- **Token refresh buffer**: 5 minutes before expiry (configurable)
- **Database**: PostgreSQL `github_installations` table (see Task 4)

**Testing Strategy:**

- Unit tests mock `@octokit/auth-app` to verify our wrapper logic (refresh timing, error handling)
- Integration tests use MSW to mock GitHub API responses for the callback flow
- No real GitHub API calls in CI — use test fixtures from GitHub's webhook payload examples

### Dependencies

**Internal Dependencies:**

- Story 1-5 (GitHub Platform Implementation): Extended with dual auth
- Story 1.5-4 (Web Server & API): Provides Fastify server for callback endpoint
- Story 1.5-6 (Webhook Integration): Routes installation lifecycle events (created/deleted/suspended)

**External Dependencies:**

- `@octokit/auth-app` — GitHub App authentication strategy
- `@octokit/rest` — GitHub REST API client (already a dependency)
- PostgreSQL 17 — Installation persistence

### Risks and Mitigations

| Risk | Severity | Mitigation |
| ---- | -------- | ---------- |
| Private key exposure in environment variable | High | Document secure env var injection (Docker secrets, K8s secrets). Never log the key. |
| Installation token refresh race condition under high concurrency | Medium | Use `@octokit/auth-app`'s built-in caching which handles concurrent refresh requests |
| GitHub App rate limits differ from PAT rate limits (5000/hr per installation vs 5000/hr per user) | Low | Document rate limit differences. Installation tokens have per-installation limits which is actually better for multi-tenant. |
| Migration conflicts with existing database schema | Medium | Use sequential migration numbering. Test migration up/down in CI. |

### Success Metrics

- [ ] All 10 acceptance criteria pass
- [ ] 100% test coverage on auth and persistence code paths
- [ ] Token refresh happens transparently — no 401 errors in steady-state operation
- [ ] Installation callback completes in < 2 seconds
- [ ] Private key never appears in logs at any level

## Related

- Related story: `docs/stories/epic-1/story-1-5/1-5-github-platform-implementation.md`
- Related story: `docs/stories/epic-1.5/story-1.5-4/1.5-4-web-server-api.md`
- Related story: `docs/stories/epic-1.5/story-1.5-6/1.5-6-webhook-integration.md`
- Related story: `docs/stories/epic-1.5/story-1.5-12/1.5-12-saas-coordinator.md`

## References

- **🔴 MANDATORY PROCESS:** [BEFORE_YOU_CODE.md](../../BEFORE_YOU_CODE.md)
- **Knowledge Base:** [.dev/README.md](../../.dev/README.md) - Search spikes, bugs, findings, decisions
- [GitHub Apps Documentation](https://docs.github.com/en/apps)
- [GitHub App Authentication](https://docs.github.com/en/apps/creating-github-apps/authenticating-with-a-github-app/about-authentication-with-a-github-app)
- [@octokit/auth-app](https://github.com/octokit/auth-app.js)

## Logging Requirements

Infrastructure and deployment components MUST use structured logging via pino/ILogger (never `console.log` in production code).

- **INFO**: Server started (mode, port, config summary), webhook received (event type, delivery ID), health check passed, config loaded/reloaded
- **DEBUG**: Request/response details (redact auth headers), environment detection, feature flag evaluation, Docker health probe
- **WARN**: Health check degraded, webhook signature verification slow, config value missing (using default), secret rotation pending
- **ERROR**: Server failed to start, webhook signature invalid, database unreachable, secret store unavailable, config parse failure
- **Structured context**: Include `{ mode, environment, serverPort, installationId }` where applicable
- **Credential safety**: NEVER log webhook secrets, API keys, JWT secrets, or private keys — log masked identifiers only
- **Startup**: Log complete config summary at INFO on startup (with secrets redacted) so operators can verify configuration
