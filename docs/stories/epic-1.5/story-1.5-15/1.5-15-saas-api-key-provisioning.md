# Story 1.5-15: SaaS API Key Provisioning & GitHub Secrets Setup

**Epic:** 1.5 - Deployment, Packaging & Operations
**Status:** Ready for Development
**Priority:** MVP Critical (SaaS Enabler)
**Estimated Effort:** 5 days

## ⚠️ MANDATORY: Before You Code

**ALL contributors MUST read and follow the comprehensive development process:**

📖 **[BEFORE_YOU_CODE.md](../../BEFORE_YOU_CODE.md)**

This mandatory guide includes:
- 7-Phase Development Workflow (Read → Research → Break Down → TDD → Quality Gates → Failure Handling)
- Knowledge Base Usage (.dev/ directory: spikes, bugs, findings, decisions)
- TRACE/DEBUG Logging Requirements for all functions
- Test-Driven Development (TDD) mandatory workflow
- 100% Test Coverage requirement
- Build Success enforcement
- Automatic retry and developer alert procedures

**Failure to follow this process will result in rework.**

## Story

As a **user who has installed the Tamma GitHub App**,
I want Tamma to automatically provision an API key into my repository's GitHub Actions secrets during setup,
so that my Actions-based workers can authenticate to the Tamma SaaS API for LLM access, workflow status updates, and result reporting — without me configuring any API keys manually.

## Overview

When a user installs the Tamma GitHub App, the installation callback (Story 1.5-11) already stores the installation and its repos in the database. This story extends that flow to also:

1. **Generate a per-installation API key** — a cryptographically random token that identifies and authenticates this installation's workers
2. **Store the key hash** in the database (never store plaintext API keys)
3. **Write `TAMMA_API_KEY` to each repo's GitHub Actions secrets** — using the GitHub REST API with libsodium sealed-box encryption
4. **Expose SaaS API endpoints** — that workers call using this key for LLM inference, workflow status, and result callbacks
5. **Handle key lifecycle** — rotation on demand, revocation on installation removal, re-provisioning when repos are added

The result: users install the Tamma GitHub App and their repos are immediately ready to run Tamma workers in GitHub Actions. Zero manual configuration.

## Acceptance Criteria

1. Installation callback generates a per-installation API key (256-bit, URL-safe base64) and stores its SHA-256 hash in the `github_installations` table
2. API key is written as `TAMMA_API_KEY` secret to every repo in the installation using GitHub's encrypted secrets API (`PUT /repos/{owner}/{repo}/actions/secrets/{secret_name}`)
3. Secret encryption uses `libsodium-wrappers` sealed-box (`crypto_box_seal`) with the repo's public key retrieved from `GET /repos/{owner}/{repo}/actions/secrets/public-key`
4. When `installation_repositories.added` webhook fires, `TAMMA_API_KEY` is provisioned to the newly added repos
5. When installation is deleted, the API key hash is removed from the database (GitHub deletes the secrets automatically when the App is uninstalled)
6. API key validation endpoint (`POST /api/v1/auth/validate`) returns installation context (installationId, accountLogin, permissions) for a valid key, 401 for invalid
7. Tamma SaaS API endpoints authenticated by `TAMMA_API_KEY` via `Authorization: Bearer <key>` header:
   - `POST /api/v1/llm/chat` — proxied LLM inference (provider selection, model, messages)
   - `POST /api/v1/workflows/{id}/status` — workflow progress updates
   - `POST /api/v1/workflows/{id}/result` — final result callback
8. LLM proxy uses the SaaS operator's provider keys (from server config), not the user's — users never need LLM API keys
9. API key can be rotated via `POST /api/v1/installations/{id}/rotate-key` (generates new key, updates all repo secrets, invalidates old key)
10. GitHub App requires `secrets: write` permission (added to App manifest)
11. Unit tests cover: key generation, hashing, secret encryption, API key validation, LLM proxy routing
12. Integration tests cover: end-to-end callback → key generation → secret provisioning → API call with key

## Tasks / Subtasks

- [ ] Task 1: API Key Generation & Storage (AC: 1)
  - [ ] Subtask 1.1: Implement `generateApiKey()` — 256-bit random bytes, URL-safe base64 encoded (`crypto.randomBytes(32).toString('base64url')`)
  - [ ] Subtask 1.2: Add `api_key_hash TEXT` column to `github_installations` table (migration 003)
  - [ ] Subtask 1.3: Store SHA-256 hash of API key: `crypto.createHash('sha256').update(key).digest('hex')`
  - [ ] Subtask 1.4: Add `api_key_prefix TEXT` column (first 8 chars of key, for identification in logs/UI without exposing the full key)
  - [ ] Subtask 1.5: Update `IGitHubInstallationStore` interface — add `updateApiKeyHash(installationId, hash, prefix)` method
  - [ ] Subtask 1.6: Update `PgInstallationStore` and `InMemoryInstallationStore` implementations

- [ ] Task 2: GitHub Secrets Provisioning (AC: 2, 3, 10)
  - [ ] Subtask 2.1: Add `libsodium-wrappers` dependency to `@tamma/api`
  - [ ] Subtask 2.2: Implement `GitHubSecretsProvisioner` class with methods:
    - `getRepoPublicKey(octokit, owner, repo)` — calls `GET /repos/{owner}/{repo}/actions/secrets/public-key`
    - `encryptSecret(publicKey, secretValue)` — libsodium `crypto_box_seal`, returns base64
    - `writeSecret(octokit, owner, repo, secretName, secretValue)` — encrypts and calls `PUT`
    - `provisionApiKey(octokit, repos, apiKey)` — writes `TAMMA_API_KEY` to all repos
  - [ ] Subtask 2.3: Create installation-scoped Octokit with `secrets: write` permission for provisioning
  - [ ] Subtask 2.4: Handle errors: repo archived (skip), insufficient permissions (log warning), rate limits (retry with backoff)
  - [ ] Subtask 2.5: Batch provisioning: process repos in parallel (max 5 concurrent) with per-repo error isolation

- [ ] Task 3: Installation Callback Extension (AC: 1, 2)
  - [ ] Subtask 3.1: Extend `registerGitHubCallbackRoute` — after storing installation and repos, generate API key
  - [ ] Subtask 3.2: Store key hash in DB via `updateApiKeyHash()`
  - [ ] Subtask 3.3: Call `GitHubSecretsProvisioner.provisionApiKey()` for all installation repos
  - [ ] Subtask 3.4: Return the API key to the user ONCE in the redirect (via query param to dashboard success page) — this is the only time the plaintext key is shown
  - [ ] Subtask 3.5: Log provisioning results (repos succeeded, repos failed, duration)

- [ ] Task 4: Webhook Handler Extension (AC: 4, 5)
  - [ ] Subtask 4.1: On `installation_repositories.added` — retrieve installation's API key hash, generate a temporary key-equivalent (or re-provision from stored key... see Dev Notes on key storage decision)
  - [ ] Subtask 4.2: On `installation_repositories.added` — provision `TAMMA_API_KEY` to newly added repos
  - [ ] Subtask 4.3: On `installation.deleted` — remove `api_key_hash` from DB (cleanup)
  - [ ] Subtask 4.4: On `installation.suspended` — no secret changes (secrets persist, but API validates suspension)

- [ ] Task 5: API Key Authentication Middleware (AC: 6)
  - [ ] Subtask 5.1: Implement `apiKeyAuthPlugin` Fastify plugin — extracts `Authorization: Bearer <key>` header
  - [ ] Subtask 5.2: Hash the provided key, look up matching installation by `api_key_hash`
  - [ ] Subtask 5.3: Reject suspended installations (403 with `installation_suspended` error)
  - [ ] Subtask 5.4: Attach `installationContext` to request (installationId, accountLogin, permissions)
  - [ ] Subtask 5.5: Rate limit per API key: 1000 req/min (configurable)

- [ ] Task 6: SaaS API Endpoints (AC: 7, 8)
  - [ ] Subtask 6.1: `POST /api/v1/llm/chat` — accepts `{ provider?, model, messages[], maxTokens?, temperature? }`, proxies to configured LLM provider using server-side API keys
  - [ ] Subtask 6.2: `POST /api/v1/workflows/{id}/status` — accepts `{ status, step, progress?, message? }`, updates workflow state in DB
  - [ ] Subtask 6.3: `POST /api/v1/workflows/{id}/result` — accepts `{ status: 'success'|'failure'|'skipped', prNumber?, error?, duration }`, finalizes workflow
  - [ ] Subtask 6.4: LLM proxy selects provider from server config (`TammaConfig.aiProviders` or `TammaConfig.agents.defaults.providerChain`)
  - [ ] Subtask 6.5: LLM proxy adds installation context to requests for per-tenant usage tracking
  - [ ] Subtask 6.6: Request validation: enforce `maxTokens` ceiling, validate model is in allowed list, reject if installation budget exceeded

- [ ] Task 7: Key Rotation (AC: 9)
  - [ ] Subtask 7.1: `POST /api/v1/installations/{id}/rotate-key` — generates new key, updates hash in DB
  - [ ] Subtask 7.2: Re-provisions `TAMMA_API_KEY` to all active repos for the installation
  - [ ] Subtask 7.3: Old key immediately invalidated (hash replaced in DB)
  - [ ] Subtask 7.4: Requires installation owner authentication (JWT or existing API key)

- [ ] Task 8: Workflow Template Update (AC: 7)
  - [ ] Subtask 8.1: Update `tamma.yml` workflow template (from Story 1.5-13) to use `TAMMA_API_KEY`:
    ```yaml
    env:
      TAMMA_API_KEY: ${{ secrets.TAMMA_API_KEY }}
      TAMMA_API_URL: https://app.tamma.dev
    ```
  - [ ] Subtask 8.2: Worker reads `TAMMA_API_KEY` from env, uses it for all Tamma API calls
  - [ ] Subtask 8.3: Worker does NOT need `ANTHROPIC_API_KEY` or any LLM provider keys — all LLM access goes through Tamma API

- [ ] Task 9: Tests (AC: 11, 12)
  - [ ] Subtask 9.1: Unit tests for `generateApiKey()` — length, format, randomness
  - [ ] Subtask 9.2: Unit tests for key hashing — deterministic, correct SHA-256
  - [ ] Subtask 9.3: Unit tests for `GitHubSecretsProvisioner` — encryption, API calls, error handling
  - [ ] Subtask 9.4: Unit tests for `apiKeyAuthPlugin` — valid key, invalid key, suspended installation, missing header
  - [ ] Subtask 9.5: Unit tests for LLM proxy — provider routing, request validation, budget enforcement
  - [ ] Subtask 9.6: Unit tests for key rotation — new key works, old key rejected, all repos re-provisioned
  - [ ] Subtask 9.7: Integration test: callback → key generation → secret provisioning (with mock GitHub API)
  - [ ] Subtask 9.8: Integration test: API key auth → LLM proxy → response (with mock LLM provider)

## Dev Notes

### ⚠️ Development Process Reminder

**Before implementing this story, ensure you have:**
1. ✅ Read [BEFORE_YOU_CODE.md](../../BEFORE_YOU_CODE.md)
2. ✅ Searched `.dev/` directory for related spikes, bugs, findings, and decisions
3. ✅ Reviewed relevant documentation in `docs/` directory
4. ✅ Checked existing code patterns for similar functionality
5. ✅ Planned TDD approach (Red-Green-Refactor cycle)

### Requirements Context Summary

**Epic Context:** This story is the linchpin that makes the SaaS model frictionless. Without it, users would need to manually configure LLM API keys in their repos — a dealbreaker for adoption. By generating and provisioning a single `TAMMA_API_KEY`, users get a zero-config experience: install the App, and their repos are immediately ready.

**Technical Context:** GitHub's Actions secrets API requires secrets to be encrypted client-side using the repo's public key and libsodium's sealed-box algorithm. The flow is:
1. `GET /repos/{owner}/{repo}/actions/secrets/public-key` → returns `{ key, key_id }`
2. Encrypt the secret using `sodium.crypto_box_seal(secret, publicKey)`
3. `PUT /repos/{owner}/{repo}/actions/secrets/TAMMA_API_KEY` with `{ encrypted_value, key_id }`

The GitHub App needs `secrets: write` permission to do this. This permission is set in the App's configuration on GitHub.

**Integration Points:**
- Story 1.5-11 (GitHub App Integration): Installation callback is extended with key generation + provisioning
- Story 1.5-6 (Webhooks): Webhook handler is extended for repo-added provisioning
- Story 1.5-13 (Actions Worker): Workflow template updated to use `TAMMA_API_KEY`
- Story 1.5-14 (Multi-Tenant Routing): API key auth provides the `installationId` for tenant routing

### Implementation Guidance

**Key Design Decisions:**

- **Hash-only storage**: The plaintext API key is NEVER stored in the database. Only the SHA-256 hash is stored. The plaintext is shown to the user exactly once (at installation time) and written to GitHub secrets. If the user loses it, they must rotate (generate a new one).

- **Per-installation, not per-repo**: One API key per installation (org/user), shared across all repos in that installation. This simplifies management — when a new repo is added, the same key is provisioned. The installation is the tenant boundary.

- **Re-provisioning on repo add**: When `installation_repositories.added` fires, we need the plaintext key to write to the new repo's secrets. Two options:
  - **Option A**: Store the plaintext key encrypted with a server-side master key (AES-256-GCM). This allows re-provisioning without user interaction.
  - **Option B**: Store only the hash. When repos are added, generate a new key (rotate), provision to ALL repos, invalidate old key.
  - **Recommended: Option A** — better UX (no surprise key rotation), and the master key is already needed for other secrets (see CLAUDE.md: "API keys encrypted at rest (AES-256)").

- **`libsodium-wrappers` over `tweetnacl`**: `tweetsodium` (GitHub's wrapper) is deprecated. `libsodium-wrappers` is the official JS binding, actively maintained, faster, and provides `crypto_box_seal()` directly.

- **LLM proxy over direct keys**: The Tamma API proxies LLM calls using the SaaS operator's own provider keys. Users never need LLM API keys. This enables:
  - Usage tracking per installation
  - Budget enforcement per tenant
  - Provider switching without touching user repos
  - Centralized rate limiting and error handling

**Technical Specifications:**

- **API key format**: 256-bit random, base64url encoded (43 chars, e.g., `tamma_sk_a1b2c3d4...`)
- **Key prefix**: `tamma_sk_` (8 chars) for identification
- **Hash algorithm**: SHA-256 (hex, 64 chars)
- **Secret name**: `TAMMA_API_KEY` (in GitHub Actions secrets)
- **Encryption**: libsodium `crypto_box_seal()` with repo's X25519 public key
- **Auth header**: `Authorization: Bearer tamma_sk_a1b2c3d4...`
- **Rate limit**: 1000 req/min per API key (configurable)
- **API key column**: `api_key_hash TEXT` (nullable, for self-hosted installations without API keys)
- **Encrypted key column**: `api_key_encrypted TEXT` (AES-256-GCM encrypted plaintext, for re-provisioning)

**Database Migration (003_api_keys.sql):**
```sql
ALTER TABLE github_installations
  ADD COLUMN api_key_hash TEXT,
  ADD COLUMN api_key_prefix TEXT,
  ADD COLUMN api_key_encrypted TEXT;

CREATE INDEX idx_installations_api_key_hash
  ON github_installations (api_key_hash);
```

**Testing Strategy:**

- Unit tests mock `libsodium-wrappers` and Octokit for encryption and API calls
- Integration tests use MSW to mock GitHub's secrets API endpoints
- API key auth tests verify the full request lifecycle (header extraction → hash → DB lookup → context injection)
- LLM proxy tests mock the upstream LLM provider and verify request routing

### Dependencies

**Internal Dependencies:**

- Story 1.5-11 (GitHub App Integration): Installation callback extended
- Story 1.5-6 (Webhook Integration): Webhook handler extended for repo-added events
- Story 1.5-13 (GitHub Actions Worker): Workflow template uses `TAMMA_API_KEY`
- Story 1.5-14 (Multi-Tenant Routing): API key provides tenant identity

**External Dependencies:**

- `libsodium-wrappers` — NaCl sealed-box encryption for GitHub secrets API
- GitHub REST API: `actions/secrets` endpoints
- GitHub App permission: `secrets: write`
- `@octokit/rest` — GitHub API client (already a dependency)

### Risks and Mitigations

| Risk | Severity | Mitigation |
| ---- | -------- | ---------- |
| GitHub App `secrets:write` permission may concern users | High | Document clearly in App description. This permission is used ONLY to write `TAMMA_API_KEY`, not to read existing secrets. GitHub's API does not allow reading secret values. |
| API key leaked in workflow logs | Medium | GitHub automatically masks `${{ secrets.* }}` values in logs. Additionally, the key has a recognizable prefix (`tamma_sk_`) for secret scanning tools to flag. |
| Race condition: repos added while provisioning is in progress | Low | Provisioning is idempotent — re-running for an already-provisioned repo just updates the secret. Use `PUT` (create or update). |
| `libsodium-wrappers` adds ~200KB to bundle | Low | Only used server-side in `@tamma/api`, not in CLI or worker bundles. Acceptable. |
| Master encryption key for stored API keys must be managed | Medium | Use `TAMMA_MASTER_KEY` env var (or `TAMMA_MASTER_KEY_FILE` for Docker secrets). Required for SaaS mode, not needed for self-hosted. |
| LLM proxy becomes a single point of failure | Medium | Standard high-availability patterns: multiple API server replicas behind load balancer. LLM proxy is stateless — any replica can handle any request. |

### Success Metrics

- [ ] All 12 acceptance criteria pass
- [ ] Zero plaintext API keys in database
- [ ] Secret provisioning succeeds for 99%+ of repos (failures logged, not fatal)
- [ ] API key validation < 5ms (DB lookup by hash index)
- [ ] LLM proxy latency overhead < 100ms (above raw provider latency)
- [ ] Key rotation completes in < 30 seconds for 100 repos

## Related

- Related story: `docs/stories/epic-1.5/story-1.5-11/1.5-11-github-app-integration.md`
- Related story: `docs/stories/epic-1.5/story-1.5-6/1.5-6-webhook-integration.md`
- Related story: `docs/stories/epic-1.5/story-1.5-13/1.5-13-github-actions-worker-mode.md`
- Related story: `docs/stories/epic-1.5/story-1.5-14/1.5-14-multi-tenant-task-routing.md`

## References

- **🔴 MANDATORY PROCESS:** [BEFORE_YOU_CODE.md](../../BEFORE_YOU_CODE.md)
- **Knowledge Base:** [.dev/README.md](../../.dev/README.md) - Search spikes, bugs, findings, decisions
- [GitHub REST API: Actions Secrets](https://docs.github.com/en/rest/actions/secrets)
- [GitHub: Encrypting secrets for the REST API](https://docs.github.com/en/rest/guides/encrypting-secrets-for-the-rest-api)
- [libsodium-wrappers npm](https://www.npmjs.com/package/libsodium-wrappers)
- [GitHub App Permissions: secrets](https://docs.github.com/en/rest/overview/permissions-required-for-github-apps#repository-permissions-for-secrets)

## Logging Requirements

Infrastructure and deployment components MUST use structured logging via pino/ILogger (never `console.log` in production code).

- **INFO**: Server started (mode, port, config summary), webhook received (event type, delivery ID), health check passed, config loaded/reloaded
- **DEBUG**: Request/response details (redact auth headers), environment detection, feature flag evaluation, Docker health probe
- **WARN**: Health check degraded, webhook signature verification slow, config value missing (using default), secret rotation pending
- **ERROR**: Server failed to start, webhook signature invalid, database unreachable, secret store unavailable, config parse failure
- **Structured context**: Include `{ mode, environment, serverPort, installationId }` where applicable
- **Credential safety**: NEVER log webhook secrets, API keys, JWT secrets, or private keys — log masked identifiers only
- **Startup**: Log complete config summary at INFO on startup (with secrets redacted) so operators can verify configuration
